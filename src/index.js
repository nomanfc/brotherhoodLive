import React from 'react';
import ReactDOM from 'react-dom';
import Brotherhood from './App';

import './index.css'

ReactDOM.render(
  <React.StrictMode>
    <Brotherhood />
  </React.StrictMode>,
  document.getElementById('root')
);
